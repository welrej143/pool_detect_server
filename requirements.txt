# Web framework
fastapi==0.116.1
uvicorn[standard]>=0.30,<0.36
python-multipart==0.0.20

# Imaging / CV
numpy>=1.26,<3
opencv-python-headless==4.10.0.84
Pillow>=10.3,<12

# Async stack helpers (pulled transitively, no need to pin unless required)
# anyio
# h11
# idna
# sniffio

# Let FastAPI pin compatible Starlette/Pydantic/annotated-types versions automatically.
